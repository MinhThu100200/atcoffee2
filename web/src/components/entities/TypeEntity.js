class TypeEntity {

  constructor(id, code, name, point, state) {
    this.id = id;
    this.code = code;
    this.name = name;
    this.point = point;
    this.state = state;
  }
}

export default TypeEntity;