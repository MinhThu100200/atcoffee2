class RewardEntity {

  constructor(id, code, name, proviso, redution, state) {
    this.id = id;
    this.code = code;
    this.name= name;
    this.proviso = proviso;
    this.redution = redution;
    this.state = state;
  }
}

export default RewardEntity;